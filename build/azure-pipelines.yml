name: $(BuildID).$(Date:yyMMdd)$(Rev:.r)

trigger:
- master

variables:
  nodeVersion: '>=10.15.3'
  npmVersion: 'latest'

jobs:
- job: Windows
  pool:
    vmImage: 'windows-2019'
    demands: npm
  steps:
    - template: ./templates/dependency-setup.yml
    - template: ./templates/build-and-run-tests.yml
